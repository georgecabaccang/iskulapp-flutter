import 'package:powersync/powersync.dart';

const sectionsTable = Table('sections', [
  Column.text('academic_year_id'),
  Column.text('grade_level_id'),
  Column.text('advisor_id'),
  Column.text('name'),
]);
