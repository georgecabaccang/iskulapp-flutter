const noOfQuestionsLabel = 'No. of Questions';
const noOfQuestionTextHint = 'Enter the total number of questions';

const aiGeneratedLabel = 'AI Generated';

const titleLabel = 'Title';
const titleTextHint = 'What should you call this assessment?';

const instructionLabel = 'Instructions';

const subjectLabel = 'Subject';

const typeLabel = 'Type';

const randomnessLabel = 'Select Randomness';

const sectionLabel = 'Select Section';
